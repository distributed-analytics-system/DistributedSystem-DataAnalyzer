'use strict';

const getAverageClicks = require('./getAverageClicks');
const getAverageTimeSpent = require('./getAverageTimeSpent');

module.exports = {
  getAverageClicks,
  getAverageTimeSpent
};
